from setuptools import setup, find_packages

setup(
    name="poselib",
    version="0.1.0",
    description="Pose library for skeleton and motion processing",
    packages=find_packages(),
    python_requires=">=3.6",
)

